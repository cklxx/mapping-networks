# Pinned to the versions this project was validated + smoke-tested on (macOS arm64,
# Python 3.11). The 4B MATH-500 run needs a CUDA GPU; the --smoke pipeline (and the
# archived phase1 FashionMNIST run) work CPU/MPS-only at these versions.
#
# Core ML packages are pinned with == to guarantee exact reproducibility of the 9B
# experiment (see experiments/math500_active_grpo_9b.py reproducibility.json).
# Auxiliary packages keep >= (floors). CUDA users: install the torch build matching
# their CUDA toolkit from https://pytorch.org (the index here is CPU/MPS-resolvable
# by default).

torch==2.12.0
transformers==5.5.0
datasets==4.5.0
accelerate==1.13.0      # transformers .from_pretrained(...).to(device) device placement
matplotlib>=3.10.8      # results/4b-math500/plot_curves.py figures
numpy==2.3.5
torchvision>=0.27.0     # experiments/archive/phase1_fmnist.py (FashionMNIST only)
